extends layout

block content
  h1= title
  p Welcome to #{title}, #{user.fullName}
  
  ul.nav
    each menu in ["Competition", "Logout"]
      li
        a(href="/#{menu}")= menu

  - for competition in competitions
      p= competition.name